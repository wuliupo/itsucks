:title Operation breakdown
:label operation breakdown

ItSucks does the following steps with every URL it finds:

1. URL found (or given initially)
2. URL is filtered by all filters and decide if the file should be downloaded and if it should be saved on disk.
3. Download and process/filter the data. That can be searching for URL's in text files or saving the data on disk.

